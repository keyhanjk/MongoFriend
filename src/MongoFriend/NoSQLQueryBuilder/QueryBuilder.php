<?php

// // 1) infix to prefix
// function infixToPrefix(string $query): array
// {

// }

// // 2) prefix to nested array (tree), evaluate prefix notation
// function prefixToTree(array $prefix): array
// {

// }

// // 3) nested array to MongoDB query
// function toMongoQuery(array $tree): array
// {

// }

// $query = "A > B and (C * D + 10 < CX * (F + G / P))";
// $prefix = infixToPrefix($query);
// $tree = prefixToTree($prefix);
// $mongoQuery = toMongoQuery($tree);

// var_dump($mongoQuery);
